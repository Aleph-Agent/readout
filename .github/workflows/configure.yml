name: Configure Pages

# Pushes the answer endpoint's credentials into the Cloudflare Pages project.
#
# Run by hand, and rarely: on first setup, and again if the Groq key is ever
# replaced. It is a separate workflow rather than a step in Pulse because the
# key does not change six times a day, and a deploy token that edits project
# settings should be used as seldom as the thing it configures changes.
#
# Why this exists at all: a Pages Function reads its secrets from the project's
# environment, which is set out of band rather than at deploy time. This is the
# out-of-band step, kept in the repository so it is reproducible and reviewable
# instead of being a thing somebody once did in a dashboard.
on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: configure-pages
  cancel-in-progress: false

jobs:
  configure:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: '24'

      - name: Put the Groq key in the Pages project
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
        run: |
          set -euo pipefail
          if [ -z "${GROQ_API_KEY:-}" ]; then
            echo "GROQ_API_KEY is not set. Nothing to configure." >&2
            exit 1
          fi
          # Piped rather than passed as an argument: an argument is visible in
          # the process list of every other process on the runner.
          printf '%s' "${GROQ_API_KEY}" \
            | npx --yes wrangler@4 pages secret put GROQ_API_KEY --project-name=readout

      - name: Put the Blockscout key in the Pages project
        # Both public JSON-RPC endpoints answer 429 from a Worker, consistently
        # — Workers egress from addresses shared with millions of sites and a
        # public RPC limits by address, so the quota is gone before this project
        # asks. A key moves the limit from the address to the key.
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          BLOCKSCOUT_API_KEY: ${{ secrets.BLOCKSCOUT_API_KEY }}
        run: |
          set -euo pipefail
          if [ -z "${BLOCKSCOUT_API_KEY:-}" ]; then
            echo "BLOCKSCOUT_API_KEY is not set. Nothing to configure." >&2
            exit 1
          fi
          printf '%s' "${BLOCKSCOUT_API_KEY}" \
            | npx --yes wrangler@4 pages secret put BLOCKSCOUT_API_KEY --project-name=readout

      - run: npm ci

      - name: Build
        env:
          CF_BEACON_TOKEN: ${{ secrets.CF_BEACON_TOKEN }}
        run: node scripts/build.ts

      - name: Redeploy
        # Load-bearing, and the reason this workflow builds at all. A Pages
        # project's environment is bound at deployment time: the secret above
        # lands in the project settings and the running deployment carries on
        # without it. Nothing changes until something new is deployed.
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: npx --yes wrangler@4 pages deploy dist --project-name=readout --branch=main

      - name: Confirm the endpoint reports itself configured
        # Checks observable behaviour rather than the setting. An unconfigured
        # endpoint answers 503 and says so, so anything else — 400 is the
        # answer to the empty question sent here — is proof the key arrived.
        run: |
          set -euo pipefail
          for attempt in 1 2 3 4 5 6; do
            code=$(curl -s -o /dev/null -w '%{http_code}' \
              -X POST https://sighttrue.com/api/ask \
              -H 'content-type: application/json' \
              --data '{"question":""}')
            echo "attempt ${attempt}: endpoint answered ${code}"
            if [ "${code}" != "503" ]; then exit 0; fi
            sleep 10
          done
          echo "Still unconfigured after six attempts. The secret did not take effect." >&2
          exit 1
