# Generated by the build. Change src/site/redirects.txt.
#
# /account was a second page titled "Your stack", reached from the account
# name while the bar pointed at /stack. Two pages doing halves of one job is
# the confusion the navigation rewrite removed, so the page is gone and its
# URL points at the one that replaced it. 301, because the merge is permanent
# and a browser that has the old address should stop asking.
/account /stack 301
